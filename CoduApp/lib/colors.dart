import 'package:flutter/material.dart';

Color blackColor = Color.fromARGB(255, 18, 18, 22);

Color whiteColor = Colors.white;

Color blueColor = Colors.blue;

Color backgroundColor = const Color.fromARGB(255, 30, 30, 30);
Color orangeColor = Colors.orange;

Color selectedItemColor = Colors.blueAccent;

Color unselectedItemColor = Colors.black.withOpacity(0.5);

Color listColor = Color.fromARGB(255, 230, 229, 249);

Color orangeRedColor = Colors.deepOrange;
